FILE(GLOB_RECURSE sources "*.cu" "*.cpp" "*.c")
SET(name "cuMPI-demo")
CUDA_ADD_EXECUTABLE(${name} ${sources})
TARGET_LINK_LIBRARIES(${name} /usr/local/cuda/lib64/libcudart.so ${MPI_LIBRARIES})
TARGET_LINK_LIBRARIES(${name} ${NCCL_LIBRARY})